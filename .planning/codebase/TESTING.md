# TESTING.md

## Frameworks
- Backend: Jest (`jest`)

## Structure
- Tests run via `npm test` (`jest --passWithNoTests` configured in package.json)

## Coverage
- No explicit coverage tools configured yet.
- Frontend testing frameworks (like Vitest or React Testing Library) are currently missing from package.json.
