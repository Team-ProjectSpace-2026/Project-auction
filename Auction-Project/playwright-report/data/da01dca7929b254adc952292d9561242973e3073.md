# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: teams.spec.ts >> Team Management >> add two teams to tournament
- Location: tests\teams.spec.ts:6:3

# Error details

```
TimeoutError: locator.click: Timeout 10000ms exceeded.
Call log:
  - waiting for getByRole('button', { name: 'Details →' }).first()

```

# Page snapshot

```yaml
- generic [active] [ref=f2e1]: Loading...
```

# Test source

```ts
  1  | import { test, expect } from "@playwright/test";
  2  | import { TEST_USER, TOURNAMENT, TEAM_1, TEAM_2 } from "./test-data";
  3  | import { registerOrLogin, getTournamentIdFromUrl } from "./helpers";
  4  | 
  5  | test.describe.serial("Team Management", () => {
  6  |   test("add two teams to tournament", async ({ page }) => {
  7  |     await registerOrLogin(page, TEST_USER);
  8  | 
  9  |     // Create tournament first
  10 |     await page.goto("/create-tournament");
  11 |     await page.locator('input[name="tournamentName"]').fill(TOURNAMENT.name);
  12 |     await page.locator('input[name="numTeams"]').fill(TOURNAMENT.teams);
  13 |     await page.locator('input[name="budgetPerTeam"]').fill(TOURNAMENT.budget);
  14 |     await page
  15 |       .locator('input[name="maxPlayersPerTeam"]')
  16 |       .fill(TOURNAMENT.maxPlayers);
  17 |     await page.locator('input[name="venue"]').fill(TOURNAMENT.venue);
  18 |     await page
  19 |       .locator('input[name="playerBasePrice"]')
  20 |       .fill(TOURNAMENT.basePrice);
  21 | 
  22 |     const tomorrow = new Date();
  23 |     tomorrow.setDate(tomorrow.getDate() + 1);
  24 |     await page
  25 |       .locator('input[name="auctionDateTime"]')
  26 |       .fill(tomorrow.toISOString().slice(0, 16));
  27 | 
  28 |     await page.getByRole("button", { name: "Create Tournament" }).click();
  29 |     await page.waitForTimeout(2000);
  30 | 
  31 |     const continueBtn = page.getByRole("button", { name: "Continue" });
  32 |     if (await continueBtn.isVisible({ timeout: 3000 }).catch(() => false)) {
  33 |       await continueBtn.click();
  34 |     }
  35 | 
  36 |     // Navigate to tournament details
  37 |     await page.goto("/tournaments");
> 38 |     await page.getByRole("button", { name: "Details →" }).first().click();
     |                                                                   ^ TimeoutError: locator.click: Timeout 10000ms exceeded.
  39 |     await page.waitForTimeout(2000);
  40 | 
  41 |     // Go to Teams tab
  42 |     await page.getByRole("button", { name: "Teams", exact: true }).click();
  43 |     await page.waitForTimeout(1000);
  44 | 
  45 |     // Add first team
  46 |     await page.getByRole("button", { name: "+ Add Team" }).click();
  47 |     await expect(page.getByText("Add New Team")).toBeVisible();
  48 |     await page.locator('input[name="teamName"]').fill(TEAM_1.name);
  49 |     await page.locator('input[name="ownerName"]').fill(TEAM_1.owner);
  50 |     await page.locator('input[name="maxPlayers"]').fill("18");
  51 |     await page.getByRole("button", { name: "Add Team", exact: true }).click();
  52 |     await page.waitForTimeout(2000);
  53 | 
  54 |     // Add second team
  55 |     await page.getByRole("button", { name: "+ Add Team" }).click();
  56 |     await expect(page.getByText("Add New Team")).toBeVisible();
  57 |     await page.locator('input[name="teamName"]').fill(TEAM_2.name);
  58 |     await page.locator('input[name="ownerName"]').fill(TEAM_2.owner);
  59 |     await page.locator('input[name="maxPlayers"]').fill("18");
  60 |     await page.getByRole("button", { name: "Add Team", exact: true }).click();
  61 |     await page.waitForTimeout(2000);
  62 | 
  63 |     // Verify both teams appear
  64 |     await expect(page.locator("body")).toContainText(TEAM_1.name);
  65 |     await expect(page.locator("body")).toContainText(TEAM_2.name);
  66 |   });
  67 | });
  68 | 
```